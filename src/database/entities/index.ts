import { Event } from './Event';
import { Plan } from './Plan';
import { Session } from './Session';
import { User } from './User';

export { User, Event, Plan, Session };

export const entities = [User, Event, Plan, Session];
